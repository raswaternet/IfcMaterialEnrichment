# Models

Sample IFC models were removed from this GitLab handoff snapshot because the original repo tracked `.ifc` files through Git LFS and the actual LFS objects were not present locally during handoff.

Use your own IFC files for testing, or restore the original sample models from the upstream GitHub/LFS source.
